services:
  api:
    build: .
    ports:
      - "8000:8000"

  demo:
    build: .
    command: uv run streamlit run src/pii_redaction/streamlit_app.py --server.address 0.0.0.0
    ports:
      - "8501:8501"
